# Bug — CLA-378 Vitality of the Tree (Barbarian / Path of the World Tree lv3, 2024)

**Verdict: FAIL** (clause 3 zero-consumer; clause 1 first-activation race + no log; offer ungated). Cores of clauses 1–2 ARE live (see evidence).

Host: DraconicDragon lv20 Barbarian, subclass set **Path of the World Tree** (wizard step 7, disk-verified `class.subclass.name`; majors[2]). EB Thug 1. lv20 `rage_damage: 4` → Life-Giving Force = 4d6.

## What WORKS (live-verified 2026-09-09)
- **Clause 1 Vitality Surge (eventually-lands):** Rage ON → change-data `DraconicDragon.tempHp = 20` (= barbarian_level), sheet header "Hit Points: 165/165 (cur/max) Temp HP: 20", ragePoints 6→5→4, activeBuffs ['Rage'], `vitalityOfTheTreeRageRound`=1 stamped. Producer: combatStanceHandler.js:267 loop `sa.triggerOnRage` → tempHpBuffHandler.grantTempHpOnRage → setTempHp.
- **Clause 2 Life-Giving Force (core):** turn-start seam fires (`vitalityOfTheTree_turn_start` collector automation/turnStartEffects.js:112 → rules/effects/turnStartEffects.js:455 sets `vitalityOfTheTreeAvailable:true` at Dragon's turn start, round 2, Rage active). Row click → VitalityOfTheTreeModal ("Choose up to 1 creatures… Each target gains 15 temp HP") → tick Thug 1 → Grant Vitality → server `Thug 1.tempHp = 15` (4d6 roll 15 ∈ [4,24], dice count = rage damage bonus EXACT) + `ability_use` log "DraconicDragon used Vitality of the Tree, granting 15 temporary hit points to Thug 1."

## Bugs
1. **Clause 3 "leftover THP vanish when Rage ends" — ZERO producer.** Rage toggle-off branch in `combatStanceHandler.js` (wasActive path, :33-58) filters activeBuffs and clears extended flag / Rage of the Gods, but NEVER strips `tempHp` from caster or previously-granted allies. Live: after rage OFF (`activeBuffs: []`), `DraconicDragon.tempHp=20` and `Thug 1.tempHp=15` persist unchanged. No `remove_temp_hp` expiration type, no pendingExpirations registrant from grantTempHpOnRage/confirmVitalityOfTheTree, nothing in expireStaleEffects/clearExpirationEffects for tempHp-on-rage-end. Fix shape: stamp self THP + each granted ally THP through a rage-anchored expiration (CLA-334 rounds recipe / `expireOnCreatureName`-style or a new `remove_temp_hp` case keyed by source) and/or strip caster tempHp at the rage-end branch; note app-wide setTempHp is replace-if-larger (tempHpService.js) so ally THP from other sources can't be told apart without attribution metadata.
2. **Clause 1 first-activation THP lost to §6-#18 full-store race.** `grantTempHpOnRage(...)` is called fire-and-forget (no await, combatStanceHandler.js:268) while activateStance's own `setRuntimeValue(activeBuffs)` POSTs a full-store snapshot; server wholesale-replaces per POST. First activation: tempHp never appeared on the server even at +7s (absent in two reads); a later write (rage-off POST carrying the accumulated client store) surfaced it. Second activation settled it permanently. Fix: single merged write (buff + rageRound + tempHp together, CLA-334 precedent) or await + refetch.
3. **No log for Vitality Surge / Rage toggle.** Campaign log got ONLY the clause-2 `ability_use` row — Rage activated/ended and the +20 THP surge are popup-only ("Rage activated", "Rage ended" / Travel-along-the-Tree modal hijack of activation). AGENTS.md logging gap.
4. **Offer ungated + RAW deviations (adjudication record):** row clickable at any time regardless of rage (`vitalityOfTheTreeAvailable` has NO UI/gate consumer); `maxTargets = roundsElapsed since rage activation` (RAW = 1 creature/turn) so round N grants N targets the SAME rolled amount; same-round-of-activation refusal popup (RAW "start of each of your turns"); `healingRange:'10 ft'` never checked (picker lists ALL combatants incl. hostile + self; gridless-lenient accepted family).

## Fix pointers
- combatStanceHandler.js: rage-end branch — strip tempHp (caster; ideally attributed ally strips); await the triggerOnRage grant loop; emit ability_use logs for rage enter/exit + surge amount.
- tempHpBuffHandler.confirmVitalityOfTheTree — register expiration (rage anchor) per target; gate row click on live Rage buff + `vitalityOfTheTreeAvailable`; enforce `rangeToFeet('10 ft')` + isWithinRange at confirm (gridless lenient); cap maxTargets at 1 to match RAW or annotate data.

## Cleanup done
Thug removed via EB/cs wipe; admin clear-change-data + clear-log; servers up. Subclass LEFT **Path of the World Tree** (CLA-385 Warrior of the Gods + CLA-394 Zealous Presence live in majors[3] Path of the Zealot — later rows must re-edit subclass themselves; no subclass grants all three).
