# Bug MA-0161 — Ancient Black Dragon "Legendary Action Uses: 3 (4 in Lair)" counter inert

**Verdict: FAIL** (identical to MA-0092/MA-0103/MA-0113/MA-0124/MA-0136/MA-0145 fingerprint — DATA-gated atop the fixed MA-0021 engine; budget never tracked, never enforced; header + Pounce + Frightful Presence fully inert text, Cloud of Insects clickable only via generic save path, entirely UNGATED by the legendary budget)

## Row
- MA-0161 · Ancient Black Dragon (`ancient-black-dragon`) · `legendary_actions[0]` header · category: legendary_actions · actionType: other.
- Row description (quoted): "Immediately after another creature's turn, the dragon can expend a use to take one of the following actions. The dragon regains all expended uses at the start of each of its turns."
- monsters.json (`ancient-black-dragon.legendary_actions[0]`, read 2026-09-14): `{name:"Legendary Action Uses: 3 (4 in Lair)", description:"Immediately after another creature's turn…"}` — header keys are exactly `[description,name]`; budget text lives only in the name; **no numeric `uses` field authored**. Cloud of Insects carries `[save_dc:21, save_type:Dexterity, damage_dice_primary:6d10, damage_type_primary:Poison, save_effect]` — generically clickable (MV-23) but NOT budget-gated. "4 in Lair" is name-text only (advisory app-wide by design, `monsterLegendaryUses.js:114` — no lair-flag consumer, CLA-325).

## Expected
Numeric legendary-uses budget (3, or 4 in lair) tracked in runtime state: counter visible on the monster card, spend decrements 3→2→1→0, cap enforced (refusal at 0 and one-per-other-creature-turn latch), all uses regain at the dragon's turn start, spends/refusals/regains logged.

## Actual (live probe, test-campaign, :5173, 2026-09-14)
- Setup: baseline before join change-data `{}` (0 keys), log `[]` (0 entries). EB search "Ancient Black Dragon" → tick → Join Encounter → `Ancient Black Dragon 1` on initiative track once (huge→Gargantuan dragon, init 10, hp 367/367, AC 22; combatSummary shows exactly one dragon instance). Card opened via initiative-card click (`.mc-overlay`).
- Card overlay: NO `.mc-legendary-counter`, NO `.mc-legendary-header-row`, no numeric uses input anywhere in DOM (`counterCount: 0` for all `*legendary-counter*`/`*legendary-header*` selectors). Header "Legendary Action Uses: 3 (4 in Lair)." renders as inert `div.mc-action` with **0 interactive children**; Pounce and Frightful Presence likewise inert (`clickables: []`). Only Cloud of Insects exposes generic `.mc-dice-link` "6d10" + `.mc-dice-link-save-clickable` "DC 21 Dexterity" — the MV-23 generic save path wired to `handleSaveRoll`, NOT the legendary gate.
- Fire-twice probe, same turn (activeCreature = AasimarTest throughout; dragon never acted; target armed AasimarTest via initiative-card target select → cs `targetName: AasimarTest` confirmed): fire #1 "DC 21 Dexterity" → save prompt "AasimarTest must make a DEXTERITY saving throw, DC 21" → Roll Save → SAVE FAILURE 14 vs 21 → resolved (save roll + 6d10=19 poison + `concentration_disadvantage_granted` automation logged). Fire #2 same turn, same other-creature turn: save prompt opened **AGAIN** with no refusal → rolled → resolved (second save roll 27 vs 21 success + second 6d10 save-damage roll logged). No legendary refusal popup, no counter decrement, zero legendary/exhausted text anywhere in UI at any point. Two spends of a "3/day"-per-turn economy on a single turn, ungated.
- (Probe hygiene note: first misclick hit the Actions-section Acid Breath "DC 22 Dexterity" AoE picker — dismissed via Skip, no save rolled, no damage; only `monsterRecharge` stamp for Acid Breath remains, unrelated to legendary.)
- Evidence sweep (post-probe): change-data keys = `AasimarTest`, `Ancient Black Dragon 1`, `__campaign__`, `__map__`, `activeCreatureName`, `combat-ui-viewingMonster*`, `combatSummary`, `lastAttack`, `pendingSavePrompts`, `pendingSaveListenerPrompts`, `saveResult-AasimarTest`, `targetEffects` — **`monsterLegendaryUses` key NEVER created** and `_legendaryUses_usedRound` latch never stamped. The only "legendary" strings in change-data are the static stat-block cache in `combat-ui-viewingMonster` (traits/legendary_actions/regional_effects text). `Ancient Black Dragon 1` runtime key holds only `_lastRollContext`/`lastSaveRoll`/`monsterRecharge`/`pendingExpirations` — no `max`/`used`. Log = 15 entries (encounter join, init roll, ability_use, two full Cloud of Insects save chains — save roll + save-damage roll each — plus generic concentration-disadvantage automation): **zero** legendary-spend/`legendary_use_refused`/regain entries. Economy entirely absent: nothing tracked, so nothing to gate. Turn-start regain never observable — the `turnStartEffects.js` `regainLegendaryUses` seam is a permanent no-op here since nothing ever stamps the map.
- Server grep: `rg -li legendary server/` = zero hits — no server-side legendary integration.

## Root cause / Likely location
1. **DATA (primary):** `public/data/monsters.json` `ancient-black-dragon.legendary_actions[0]` lacks `uses: 3`. The MA-0021 economy is header-data-gated: `src/services/encounters/monsterLegendaryUses.js:127 legendaryHeaderAction` — `return rows[0]?.uses != null ? rows[0] : null` → null here; `legendaryMaxUses` (:131) then returns null (no stamped max either) → `legendaryExpendGate` can never engage (`no-uses`).
2. **Render:** `MonsterCardBody.jsx` actionSections map — gated branch requires `s.key==='legendary_actions' && legendaryHeader`; with no header `uses` the section takes the generic fallback (no `MonsterLegendaryHeaderRow`, no `legendaryGate`), so Cloud of Insects is clickable via handleSaveRoll UNGATED and header/Pounce/Frightful Presence are inert text (MV-17 fingerprint).
3. Turn-start regain seam IS live (`turnStartEffects.js` `regainLegendaryUses`) but a permanent no-op — nothing ever stamps `monsterLegendaryUses` for this dragon.

Context: byte-identical failure mode to MA-0092 (Adult Copper) / MA-0103 (Adult Gold) / MA-0113 (Adult Green) / MA-0124 (Adult Red) / MA-0136 (Adult Silver) / MA-0145 (Adult White) — `ancient-black-dragon` sits in the unamended set of headers lacking `uses`. Registry: `docs/test-monster-registry.json` has an `Ancient Black Dragon` entry from MA-0160 (name `Ancient Black Dragon 1`, hp 367, AC 22) — re-Join performed this probe (init varies: registry init 4, this run init 10; hp/AC match). Registry re-entry/update needed by orchestrator.

## Steps to Reproduce
1. test-campaign → Encounters → search "Ancient Black Dragon" → tick → Join Encounter (verified 2026-09-14; lands init 10, hp 367, AC 22).
2. Open the dragon's initiative-card overlay: no uses counter; "Legendary Action Uses: 3 (4 in Lair)", Frightful Presence and Pounce inert `div.mc-action` (0 interactive children); Cloud of Insects shows generic 6d10 / DC 21 Dex save links.
3. Arm AasimarTest on the dragon card, click "DC 21 Dexterity" (Cloud of Insects) twice on the same other-creature turn: save prompt resolves each time (save roll + 6d10 damage logged); no refusal, no decrement, no `monsterLegendaryUses` in change-data ever; log contains zero legendary/refusal/regain entries.

## Notes / design gaps
- Fix is data-shaped (MA-0021 recipe): author `uses: 3` (+ optional lair note) on the header row — counter, gated spend, one-per-other-turn latch, refusal logs, turn-start regain then activate unchanged.
- Frightful Presence (Fear via Spellcasting) and Pounce (Rend attack) have no save/dice fields — even after `uses` is authored they remain inert rows unless the legendaryGate click-to-spend applies per the MA-0021 verbatim-row behavior; Cloud of Insects is already clickable and would route through the gate once the header is authored.
- "4 in Lair" remains advisory-only app-wide by design (`monsterLegendaryUses.js:114`); not a blocker for the base-3 economy.
- Cosmetic: `Legendary Resistance (4/Day, or 5/Day in Lair)` trait is inert display text too (separate 1/day-style economy, out of scope for this row); lair_actions rows with empty names render stray "." (MV-24 fingerprint) — out of scope.

## Cleanup
- Browser closed; POST `/api/campaigns/test-campaign/admin/clear-change-data` + `/admin/clear-log` (Host localhost); verified change-data `{}` + log `[]` (0 entries). No manifest `verified` edits. Registry not modified (orchestrator owns registry/manifest).
