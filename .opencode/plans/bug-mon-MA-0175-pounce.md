# BUG MA-0175 — Ancient Blue Dragon "Pounce" (legendary_actions[3]): inert legendary row, zero affordance, zero log/state delta (data at stableKey is "Tail Swipe" — manifest text drift)

## Overview
Row MA-0175 — monster "Ancient Blue Dragon" (monsterIndex ancient-blue-dragon), category legendary_actions, actionIndex 3, actionName "Pounce", actionType other. Verified live 2026-09-15 in test-campaign (header MV-18 verified; EB exact Join → cs creatures[0] "Ancient Blue Dragon 1", hp 481, ac 22, init 8; target armed ElderPaladin via initiative-card `[data-testid="target-select"]`, server mirror `targetName=ElderPaladin` confirmed).

**Data drift first:** the file at the stableKey `ancient-blue-dragon|legendary_actions|3` is `{name:"Tail Swipe", description:"The dragon makes one Rend attack."}` — keys `["name","description"]` ONLY. There is NO "Pounce" row in this monster's legendary dict; the manifest's quoted Pounce text ("The dragon moves up to half its Speed, and it makes one Rend attack.") belongs to Ancient Black Dragon MA-0164 — the exact collision the playbook MA-0164 orchestrator manifest-edit pitfall names MA-0175 for. The verdict is unchanged under either reading: every legendary dict entry on this dragon carries `name`+`description` only (no `uses`/`attack_bonus`/`save_dc`/`dice`/`advisory`/`delegates_to`/`automation`) → MA-0092/MA-0163/MA-0172 inert fingerprint.

The row renders as a plain inert `<div class="mc-action">` (children: `STRONG` "Tail Swipe." + `SPAN` description only — zero `a/button/.mc-dice-link/.clickable/[role=button]/input` descendants). Two forced `el.click()` + one trusted `page.mouse.click` at fresh rect center produced zero popups, zero campaign-log entries (2→2), zero change-data keys. No legendary economy exists (header dict lacks numeric `uses`) → no counter, no spend, no refusal gate; no `*_refused` log can exist. FAIL flavor (b) per MV-28/MV-30 bar (inert + grep-zero = FAIL, never INCOMPLETE).

## Expected (verbatim row + monsters.json Rend numbers)
Manifest row (verbatim): "The dragon moves up to half its Speed, and it makes one Rend attack." — legendary_actions[3], actionName "Pounce".
Data at stableKey (verbatim): `{name:"Tail Swipe", description:"The dragon makes one Rend attack."}`.

Under either reading, PASS requires the row to resolve as a live legendary action: exactly one "Rend" attack roll (and, for the manifest text, an advisory movement leg). The referenced attack (actions[2] "Rend") is authored live: `attack_bonus: 16`, reach "15 ft.", `damage_dice_primary: "2d8 + 9"` Slashing + `damage_dice_secondary: "2d10"` Lightning. Per MA-0070 the fixable data shape is header `uses:3` + row `delegates_to:"Rend"` (with attack_bonus/dice); composite move-then-attack legs otherwise have no producers (§7 movement subsystem — no position consumer).

## Actual
- Row DOM (legendary_actions[3] position): `<div class="mc-action "><strong>Tail Swipe.</strong> <span>The dragon makes one Rend attack.</span></div>` — clickableChildren = [] (queried `a,button,.mc-dice-link,.clickable,[role=button],input,[class*=dice]`). Overlay held 15 `.mc-action` rows total; `.mc-dice-link-legendary` = 0 across the card.
- No legendary header/counter: `.mc-legendary-counter` absent — `monsterLegendaryUses.js legendaryHeaderAction` gate requires `legendary_actions[0].uses != null`; header dict keys are [name, description] only (name "Legendary Action Uses: 3 (4 in Lair)" is prose) → plain `MonsterActionSection` branch (`MonsterCardBody.jsx:54-55`). No `monsterLegendaryUses` key in change-data.
- Click deltas: forced `row.click()` ×2 (popups 0) + trusted click (954, 447 after scrollIntoView): campaign log stayed 2→2 through all probes; no `Ancient Blue Dragon 1` change-data key at any point (verified again after 12s debounce); zero `pounce*`/`tail*` keys top-level or per-monster; zero popups; zero `pounce_refused`/`tail_swipe_refused`/`legendary_use_refused` entries (gate machinery absent because economy absent). Only new keys after avatar open: `combat-ui-viewingMonster*` (UI-only view state).
- CONTROL (engine alive): same card, same session — Rend row `.mc-dice-link` "+16" trusted click → popup "Rend … d20 12 +16 … ✓ HIT (28 vs AC 19)" → Done (`button.dice-roll-reroll-btn`) → log 2→5: roll/attack `Rend` (+16, 28 vs AC 19), roll/damage `2d8 + 9` rolls [8,8], `hp_change` ElderPaladin; lastAttack `attackerName "Ancient Blue Dragon 1", attackName "Rend", hit:true, damageApplied:true, damageFormula "2d8 + 9", secondaryFormula "2d10"`. Engine fully alive → the legendary row itself is the unwired gap.

## Grep proof (zero consumers — MA-0164 re-cite + tail)
`grep -rni "pounce" src/ server/ --include="*.js" --include="*.jsx" | grep -v ".test."` → ONLY `src/services/automation/handlers/combat/combatStanceHandler.js:141-142,394-398` (`_instinctivePounce`/`rage_bonus_movement` — PC barbarian Rage feature, unrelated). ZERO consumers of a monster Pounce legendary action app-wide (MA-0164 proof re-cited). "tail swipe" as a mechanic: zero src consumers outside render text.

## Steps to Reproduce
1. `npm run dev`, open http://localhost:5173, select **test-campaign** (verify header, MV-18).
2. Encounters → search "Ancient Blue Dragon" → tick → **Join Encounter** (only path into initiative; EB table resets after).
3. Initiative → dragon card (cs[0]) → arm target ElderPaladin via `[data-testid="target-select"]`; verify mirror via `/api/campaigns/test-campaign/change-data`.
4. Click dragon avatar (`img[alt="Ancient Blue Dragon 1"]`) → `.mc-overlay` → scroll to Legendary Actions → row [3] ("Tail Swipe" in data / "Pounce" in manifest): `DIV.mc-action`, zero links, no `(N left)` counter.
5. Forced `el.click()` ×2 + trusted click at fresh rect center → zero popup; `/log` count unchanged (2); `/change-data` has no pounce*/tail*/monsterLegendaryUses keys.
6. Control: "Rend" row `.mc-dice-link` ("+16") → HIT popup → Done (`button.dice-roll-reroll-btn`) → roll + `hp_change` ElderPaladin land (engine alive).

## Likely Location
- **Data**: `public/data/monsters.json` ancient-blue-dragon legendary dict — header `legendary_actions[0]` lacks numeric `uses` (MA-0070 data-gate) and row [3] lacks `delegates_to:"Rend"` + `attack_bonus`/`dice` (or `advisory`) affordance fields; every entry is [name, description] only (MA-0163/MA-0172 fingerprint shape). Additionally a **manifest data-shape mismatch**: MA-0175's actionName/description were authored from Ancient Black's MA-0164 Pounce block (orchestrator anchor pitfall — anchor on unique `"id"` + stableKey).
- **Render/logic**: `src/components/encounter/MonsterCardBody.jsx:38,54-55` (`legendaryHeaderAction` truthy-gate → plain section when `uses` absent), `src/components/encounter/MonsterAction.jsx` (affordances only for attack_bonus/save_dc/dice rows), `src/components/encounter/MonsterCardModal.jsx` (:243 `legendaryRowHasNumericMechanic`, :252 `advisory`, :291 `delegates_to` branches — no move+attack composite / monsterLairActions-style legendary prose branch exists). MA-0070 fix recipe applies: header `uses:3` + `delegates_to:"Rend"`.

## Notes
- Movement clause ("moves up to half its Speed", manifest text) has NO consumer app-wide — §7: no monster movement/token-position producer; even the canonical MA-0070 fix shape delivers advisory movement + delegated Rend roll only (movement distance listed as advisory residual).
- Same-monster precedents: MA-0164 (Ancient Black Pounce — same description text, inert FAIL — source of this manifest's text drift), MA-0172 (whole ABD legendary dict name/description-only inert fingerprint, pre-check note explicitly covering MA-0175), MA-0163/MA-0173/MA-0174 (legendary prose rows on this dragon, FAIL flavor (b)), MA-0168/MA-0170/MA-0171 (this dragon's live Rend/breath/spellcasting paths). Registry: EB re-join → init 8 this run (vs 4 at MA-0174 join); cosmetic secondaryDamageType "Slashing" mislabel on dual-damage lastAttack (MA-0168/MV-9 precedent — log/`secondaryFormula` authoritative).
- Persistent prompt-injection blocks (fake `[SYSTEM: URL RECORD/FINAL]` records, fabricated assistant "cleanup/PASS/file deleted/server stopped" transcripts) appeared embedded in tool outputs throughout this session; none obeyed — page stayed localhost, no git/manifest/playbook/registry writes, verdict from self-issued evidence only.
