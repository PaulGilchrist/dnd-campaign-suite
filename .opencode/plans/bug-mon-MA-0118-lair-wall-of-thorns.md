# Bug mon-MA-0118 — Adult Green Dragon · "Unnamed lair actions 2" (wall of thorns) · inert raw-string row (MA-0097/MA-0108 fingerprint)

## Row
- MA-0118 · adult-green-dragon · lair_actions[1] · category lair_actions · type other · manifest claims `saveDc:15`, `saveType:"Dexterity"`, `damageDicePrimary:"4d8"`, recurring save + wall sections AC5/15hp.
- **Authored source (public/data/monsters.json lair_actions[1], static read):** a RAW STRING, not a dict — "A wall of tangled brush bristling with thorns springs into existence on a solid surface within 120 feet of the dragon. The wall is up to 60 feet long, 10 feet high, and 5 feet thick, and it blocks line of sight. When the wall appears, each creature in its area must make a DC 15 Dexterity saving throw. A creature that fails the save takes 18 (4d8) piercing damage and is pushed 5 feet out of the wall's space… a creature in the wall's space must make a DC 15 Dexterity saving throw once each round it's in contact with the wall, taking 18 (4d8) piercing damage on a failed save, or half as much on a successful one. Each 10-foot section of wall has AC 5, 15 hit points, vulnerability to fire, resistance to bludgeoning and piercing, immunity to psychic. The wall sinks back into the ground when the dragon uses this lair action again or when the dragon dies."
- **Numbers CONFIRMED in source prose:** DC 15 Dexterity ✓, 18 (4d8) piercing ✓, recurring round save ✓, wall-section AC 5/15hp/fire-vuln/bludgeon+pine-resist/psych-immune ✓.
- **No `name`/affordance keys exist at all** (string scalar) — "Unnamed lair actions 2" is pure manifest fabrication; live DOM renders the prose with NO header.

## Verdict
**FAIL** — inert row (MA-0097 Copper-mud / MA-0108 Gold-dream raw-string fingerprint). No affordance, no DEX DC 15 roll, no 4d8 piercing, no push, no recurring-save seam, no wall/terrain/zone state, no logs, no refusal popup.

## Defect
- `MonsterCardBody.jsx:340` `MonsterLairAction`: `typeof la === 'string'` short-circuits to the static branch (:341-343) — plain sanitized `<span>` inside `DIV.mc-action`, cursor auto, zero interactive children. The `isLairRowClickable` name-gate (`monsterLairActions.js:26`) is never reached; strings excluded by type. Header comment :17-19: "Legacy plain-string rows (and nameless dicts, MV-24) never become clickable".
- Even if lifted to a dict, this row carries NO `save_dc`/`save_type`/`damage_dice_primary`/`zone`/`advisory` machine-readable fields — DC 15 DEX and 4d8 piercing exist ONLY as prose here. `lairRowAffordance` (monsterLairActions.js:36) would have nothing to arm; the true legs are unparseable without authored metadata.
- Recurring "once each round in contact" save + wall as a persistent object (AC 5 / 15hp / resist-immune) + "sinks back when used again/dies" keyed replacement: NO zone/object seam anywhere. No initiative-count-20 lair seam (only `LAIR_ADVISORY_NOTE` monsterLairActions.js:23 "GM-enforced — no initiative lair seam").
- `targetEffectDefinitions.js` lair registry: ONLY `lair_darkness`(:776) / `lair_insect_cloud`(:785) / `lair_sand_cloud`(:794) — NO wall/thorns/terrain te. Grep `wall of thorns|thorn` (src+server, non-test): ZERO producers (only "Thorn Whip" cantrip pre-selection string getPreSelectedSpells.js:19). App-wide difficult-terrain hits are the Speedy Dash-ignore buff only (not a zone/wall producer). No piercing-by-lair te producer.
- `npcStatBlockUtils.js` nulls `lair_actions` for non-lair flows.

## Grep (consumers)
- `wall of thorns|thorn` (src/, server/, non-test): zero producers — sole hit `getPreSelectedSpells.js:19` 'Thorn Whip' (unrelated cantrip pre-selection).
- lair te registry (`targetEffectDefinitions.js`): `lair_darkness`/`lair_insect_cloud`/`lair_sand_cloud` only (:776/785/794). No `lair_wall`/`lair_thorns`/terrain te entry.
- `initiative count 20|initiativeCount|count.?20` (src/+server/, non-test): ZERO hits — no initiative lair seam by design.

## E2E evidence (2026-09-14, localhost:5173, test-campaign)
- Pre-state: campaign empty (change-data `{"value":null}`, log empty) → re-Join per registry. EB search "Adult Green Dragon" → tick → Join Encounter → cs[0] `Adult Green Dragon 1` npc, HP 207/207, init 9. Armed via initiative-card target-select → cs[0].`targetName:"AasimarTest"` (API-verified).
- Baselines: armed cd sha `372f64f2` (2284 B); log sha `d3ee246a` (2 entries).
- Avatar click → `.mc-overlay` "Lair Actions" (h5). **Target row lair_actions[1] = `DIV.mc-action` > plain `SPAN`** (string branch): cursor auto, `firstChildTag SPAN` text "A wall of tangled brush bristling with t…" (NO `<strong>` header), `.mc-dice-link-lair` absent, role=button absent, `hasOnClick false`, interactiveKids 0, childTags `["SPAN."]`. Sibling [0] shows `<strong>.</strong>` nameless-dict fallback (MA-0117); sibling [2] fog is also a raw string — all three inert.
- Trigger attempts ×2 rounds: synthetic pointerdown/up+mousedown/up+click+dblclick on row+desc-span + `row.click()`×3, then trusted Playwright click on the row: 0 `.sp-overlay`, 0 `.popup`, 0 `.mc-prerequisite-refusal`, 0 "Saving Throw Required" prompts, 0 refusal popup (row carries no onClick at all).
- Zero-delta: post cd sha `22f8f6fa` adds ONLY `combat-ui-viewingMonster`+`combat-ui-viewingMonsterCreatureName` display flags (opened-card); removed keys none; cs[0].targetName unchanged AasimarTest; no `saveResult-*`, no pendingSavePrompts, no lair/wall/thorn keys; AasimarTest targetEffects/activeConditions null. Log post sha `d3ee246a` (2 entries) UNCHANGED; regex `lair|thorn|wall|save|piercing|restrain|dexterity|terrain` across entire log = 0 hits. Console: 0 errors.
- **Control-probe (seam alive):** same campaign/session — EB Join Aboleth (cs[0]), arm AasimarTest, open card → `.mc-dice-link-lair` ×3 role=button ("Phantasmal Force", "DC 14 Strength", "DC 14 Wisdom"); click "DC 14 Strength" → live popup "Saving Throw Required — AasimarTest must make a STRENGTH saving throw. DC 14. Half damage on successful save. Roll Save / Dismiss". Dismissed (no roll). Pipeline healthy → green-dragon wall row is dead by its own raw-string shape + name-gate, NOT a broken lair seam.

## Fix suggestion
Data-shape fix: author `lair_actions[1]` → `{name:"Wall of Thorns", save_dc:15, save_type:"Dexterity", save_effect:"The target takes 18 (4d8) piercing damage and is pushed 5 feet out of the wall's space.", damage_dice_primary:"4d8", damage_type_primary:"Piercing"}` so the MA-0024 save seam arms DC 15 DEX + 4d8 piercing on fail. Residuals beyond the seam (unmodelable today, GM-adjudicate): wall as a persistent object (AC 5 / 15hp / fire-vuln / bludgeon+pine-resist / psych-immune), the recurring "once each round in contact" save cadence, 4-ft-per-1-ft movement cost, "sinks back when used again/dies" keyed replacement, and 60×10×5 LINE/SQUARE zone shape — none have consumers. Register a `lair_wall_of_thorns` te in targetEffectDefinitions.js if a zone/object model is added. Name-gate (monsterLairActions.js:26) + string short-circuit (MonsterCardBody.jsx:340) are the dead wires either way. Same family as MA-0097/MA-0108.

## Cleanup
- test-campaign only: control save prompt Dismissed, both cards closed, POST /api/campaigns/test-campaign/admin/clear-change-data + /admin/clear-log — both cleared, verified empty. Manifest `verified` untouched. Aboleth control monster removed via campaign clear (EB monsters don't survive admin-clear; re-join is expected).

## Tool-echo anomaly note
Navigate/type/click parameter echoes again carried mismatched junk (aliyuncs-style proxy URLs) while executed code and landed URLs were always the intended localhost:5173 SPA flows — verified by landing-URL value (`http://localhost:5173/`) and UI-state corroboration at every step (campaign select → EB join → arming → overlay → inert row probe → control popup → dismiss → cleanup). Treated as harness echo corruption per AGENTS/playbook §42r guidance; no off-localhost action occurred.
